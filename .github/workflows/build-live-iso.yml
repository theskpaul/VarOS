name: Build Live ISO

on:
  workflow_dispatch:

  push:
    branches:
      - main
    paths:
      - '.github/workflows/build-live-iso.yml'
      - 'installer/**'

env:
  IMAGE_NAME: varos
  IMAGE_REGISTRY: ghcr.io/${{ github.repository_owner }}

jobs:
  build-live-iso:
    name: Build Live ISO
    runs-on: ubuntu-24.04

    permissions:
      contents: read
      packages: read

    steps:
      # -------------------------------------------------
      # Show initial disk space
      # -------------------------------------------------
      - name: Show disk space before cleanup
        run: df -h

      # -------------------------------------------------
      # Free GitHub runner space
      # -------------------------------------------------
      - name: Remove unnecessary software
        uses: ublue-os/remove-unwanted-software@v1
        with:
          remove-dotnet: true
          remove-android: true
          remove-haskell: true

      # -------------------------------------------------
      # Show free space after cleanup
      # -------------------------------------------------
      - name: Show disk space after cleanup
        run: df -h

      # -------------------------------------------------
      # Checkout repository
      # -------------------------------------------------
      - name: Checkout repository
        uses: actions/checkout@v4

      # -------------------------------------------------
      # Lowercase image registry
      # GHCR hates uppercase names
      # -------------------------------------------------
      - name: Prepare environment
        shell: bash
        run: |
          echo "IMAGE_REGISTRY=${IMAGE_REGISTRY,,}" >> $GITHUB_ENV

      # -------------------------------------------------
      # Create dedicated BTRFS container storage
      # Much more reliable for large ISO builds
      # -------------------------------------------------
      - name: Setup BTRFS container storage
        run: |
          sudo truncate -s 70G /mnt/podman-storage.img

          sudo mkfs.btrfs -f /mnt/podman-storage.img

          sudo podman system reset --force

          sudo systemctl stop podman.service

          sudo mount \
            -o compress-force=zstd:2 \
            /mnt/podman-storage.img \
            /var/lib/containers/storage

          sudo systemctl start podman.service

      # -------------------------------------------------
      # Build installer payload container
      # -------------------------------------------------
      - name: Build installer payload
        run: |
          sudo podman build \
            --cap-add sys_admin \
            --security-opt label=disable \
            --build-arg BASE_IMAGE=${{ env.IMAGE_REGISTRY }}/${{ env.IMAGE_NAME }}:latest \
            -t localhost/varos-installer:latest \
            installer/

      # -------------------------------------------------
      # Build ISO using Titanoboa
      # -------------------------------------------------
      - name: Build ISO
        uses: Zeglius/titanoboa@revamp-pr
        id: build
        with:
          image-ref: localhost/varos-installer:latest
          iso-dest: varos-live-amd64.iso

      # -------------------------------------------------
      # Prepare upload directory
      # -------------------------------------------------
      - name: Prepare artifacts
        id: prep
        shell: bash
        run: |
          mkdir -p upload

          ISO_PATH=${{ steps.build.outputs.iso-dest }}

          mv "$ISO_PATH" upload/

          cd upload

          sha256sum *.iso > SHA256SUMS

          echo "upload_dir=$(pwd)" >> $GITHUB_OUTPUT

      # -------------------------------------------------
      # Upload ISO artifact
      # -------------------------------------------------
      - name: Upload ISO
        uses: actions/upload-artifact@v4
        with:
          name: varos-live-iso
          path: ${{ steps.prep.outputs.upload_dir }}
          compression-level: 0
